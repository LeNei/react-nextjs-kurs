"use client";

// Die error Datei wird immer dann aufgerufen, wenn in der page Datei ein 
// Error geworfen wird. Dies kann vor allem dabei helfen, wenn beim Laden von
// Daten ein Fehler auftritt
export default function Error() {
  return <p>Fehler!!</p>;
}
